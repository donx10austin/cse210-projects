<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- This makes sure scriptures.txt is copied into bin/Debug/net8.0 automatically -->
  <ItemGroup>
    <None Include="scriptures.txt" CopyToOutputDirectory="Always" />
  </ItemGroup>

</Project>